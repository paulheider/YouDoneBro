RootWidget:
    size_hint: 1,1
    
    Label:
        id: facilitators
        text: 'facilitators'
        size_hint: 0.5,0.1
        pos_hint: {'center_x': 0.5 , 'top': (self.parent.height + 1.75*(5 + self.parent.height*0.25) - (self.parent.height/2)) / self.parent.height }
    Button:
        id: fac_dude_btn
        text: 'a dude'
        size_hint: 0.25,0.1
        pos_hint: {'center_x': (self.parent.width - 5 - 0.5*(self.parent.width*0.25) - (self.parent.width/2)) / self.parent.width , 'top': (self.parent.height + 1.5*(5 + self.parent.height*0.25) - (self.parent.height/2)) / self.parent.height }
        on_press: self.parent.tap( 'fac_dude' )
    Label:
        id: fac_dude_time
        text: '00:00:00'
        size_hint: 0.125,0.1
        pos_hint: {'center_x': (self.parent.width - 5 - 0.75*(self.parent.width*0.25) - (self.parent.width/2)) / self.parent.width , 'top': (self.parent.height + 1*(5 + self.parent.height*0.25) - (self.parent.height/2)) / self.parent.height }
    Label:
        id: fac_dude_pct
        text: '0%'
        size_hint: 0.125,0.1
        pos_hint: {'center_x': (self.parent.width - 5 - 0.25*(self.parent.width*0.25) - (self.parent.width/2)) / self.parent.width , 'top': (self.parent.height + 1*(5 + self.parent.height*0.25) - (self.parent.height/2)) / self.parent.height }
    Button:
        id: fac_nd_btn
        text: 'not a dude'
        size_hint: 0.25,0.1
        pos_hint: {'center_x': (self.parent.width + 5 + 0.5*(self.parent.width*0.25) - (self.parent.width/2)) / self.parent.width , 'top': (self.parent.height + 1.5*(5 + self.parent.height*0.25) - (self.parent.height/2)) / self.parent.height }
        on_press: self.parent.tap( 'fac_nd' )
    Label:
        id: fac_nd_time
        text: '00:00:00'
        size_hint: 0.125,0.1
        pos_hint: {'center_x': (self.parent.width + 5 + 0.75*(self.parent.width*0.25) - (self.parent.width/2)) / self.parent.width , 'top': (self.parent.height + 1*(5 + self.parent.height*0.25) - (self.parent.height/2)) / self.parent.height }
    Label:
        id: fac_nd_pct
        text: '0%'
        size_hint: 0.125,0.1
        pos_hint: {'center_x': (self.parent.width + 5 + 0.25*(self.parent.width*0.25) - (self.parent.width/2)) / self.parent.width , 'top': (self.parent.height + 1*(5 + self.parent.height*0.25) - (self.parent.height/2)) / self.parent.height }

    Button:
        text: 'pause'
        size_hint: 0.25,0.1
        pos_hint: {'center_x': 0.5 , 'top' : (self.parent.height + 0.5*(5 + self.parent.height*0.25) - (self.parent.height/2)) / self.parent.height }
        on_press: self.parent.tap( 'pause' )

    Button:
        id: nf_dude_btn
        text: 'a dude'
        size_hint: 0.25,0.1
        pos_hint: {'center_x': (self.parent.width - 5 - 0.5*(self.parent.width*0.25) - (self.parent.width/2)) / self.parent.width , 'top': (self.parent.height - 0.5*(5 + self.parent.height*0.25) - (self.parent.height/2)) / self.parent.height }
        on_press: self.parent.tap( 'nf_dude' )        
    Label:
        id: nf_dude_time
        text: '00:00:00'
        size_hint: 0.125,0.1
        pos_hint: {'center_x': (self.parent.width - 5 - 0.75*(self.parent.width*0.25) - (self.parent.width/2)) / self.parent.width , 'top': (self.parent.height - 0*(5 + self.parent.height*0.25) - (self.parent.height/2)) / self.parent.height }
    Label:
        id: nf_dude_pct
        text: '0%'
        size_hint: 0.125,0.1
        pos_hint: {'center_x': (self.parent.width - 5 - 0.25*(self.parent.width*0.25) - (self.parent.width/2)) / self.parent.width , 'top': (self.parent.height - 0*(5 + self.parent.height*0.25) - (self.parent.height/2)) / self.parent.height }

    Button:
        id: nf_nd_btn
        text: 'not a dude'
        size_hint: 0.25,0.1
        pos_hint: {'center_x': (self.parent.width + 5 + 0.5*(self.parent.width*0.25) - (self.parent.width/2)) / self.parent.width , 'top': (self.parent.height - 0.5*(5 + self.parent.height*0.25) - (self.parent.height/2)) / self.parent.height }
        on_press: self.parent.tap( 'nf_nd' )
    Label:
        id: nf_nd_time
        text: '00:00:00'
        size_hint: 0.125,0.1
        pos_hint: {'center_x': (self.parent.width + 5 + 0.75*(self.parent.width*0.25) - (self.parent.width/2)) / self.parent.width , 'top': (self.parent.height - 0*(5 + self.parent.height*0.25) - (self.parent.height/2)) / self.parent.height }
    Label:
        id: nf_nd_pct
        text: '0%'
        size_hint: 0.125,0.1
        pos_hint: {'center_x': (self.parent.width + 5 + 0.25*(self.parent.width*0.25) - (self.parent.width/2)) / self.parent.width , 'top': (self.parent.height - 0*(5 + self.parent.height*0.25) - (self.parent.height/2)) / self.parent.height }
    Label:
        text: 'non-facilitators'
        size_hint: 0.5,0.1
        pos_hint: {'center_x': 0.5 , 'top': (self.parent.height - 0.75*(5 + self.parent.height*0.25) - (self.parent.height/2)) / self.parent.height }
